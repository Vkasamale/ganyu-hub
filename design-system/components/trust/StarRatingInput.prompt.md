Collects a rating. 28px stars with a hover preview and a 1.1 scale on the hovered star.

```jsx
<StarRatingInput name="rating_quality" onChange={(n) => setQuality(n)} />
```

Bigger than the read-only `Stars` on purpose: this is a target, not a label.
