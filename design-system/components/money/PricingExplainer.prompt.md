Fee disclosure, collapsed by default, on a wash panel inside a card. A native `<details>` — no JS, works before hydration, which matters on a slow connection.

```jsx
<PricingExplainer audience="creative" />
```

Keep the copy: it names who charges what ("Cash-out fees are charged by the payment provider, not Ganyu Hub"). The reason it reads defensively is that "where did my money go?" is the question this product exists to prevent.
