Field label. Always present — Ganyu Hub does not use placeholder-only fields, because the label disappearing on a mid-range Android keyboard is how people mis-fill a payment form.

```jsx
<Label htmlFor="budget">Your budget</Label>
```
