The standard text field — 40px tall, white on paper, neutral-300 border, teal focus ring.

```jsx
<Label htmlFor="title">What do you need done?</Label>
<Input id="title" placeholder="Logo for a bakery in Blantyre" />
```

Pair with `Label` above and a 12px hint below. For MWK amounts use `MoneyInput` instead — it formats thousands as you type.
