The brand lockup — always the mark plus the word, in that order, left-aligned.

```jsx
<Logo size="md" markSrc="../../assets/logo-g.png" />
```

"Ganyu" is ink; "Hub" is ink at 60%. Inter at 600, tracking -0.025em. The mark is a stamp — dashed outer ring, teal G — and is always rendered circular (`border-radius: 9999px`), never on a coloured plate. Never redraw it; use `assets/logo-g.png`.
